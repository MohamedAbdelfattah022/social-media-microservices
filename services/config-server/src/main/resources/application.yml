server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest

#management:
#  metrics:
#    tags:
#      app: ${spring.application.name}
#
#  endpoints:
#    web:
#      exposure:
#        include: "*"

#spring.cloud.bus:
#  enabled: true
#  refresh:
#    enabled: true
